# PostgreSQL Configuration
POSTGRES_USER=planeats_user
POSTGRES_PASSWORD=your_strong_password_here # Change this in your actual .env file
POSTGRES_DB=planeats_db

# Backend Configuration (example)
# SECRET_KEY=your_fastapi_secret_key # For JWT, etc.
# GEMINI_API_KEY=your_gemini_api_key

# Frontend Configuration (if any needed directly by docker-compose, usually not)
# NEXT_PUBLIC_SOME_VARIABLE=value